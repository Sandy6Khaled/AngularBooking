export type Room = {
    id: number,
    //roomType: number,
    // description: string,
    price: number,
    //numberOfBeds: number,
    // reservation: any,
    // reservationId: any,
    // hotelId: number,
    // packages: any[]
}